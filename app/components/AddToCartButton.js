/**
 * app/components/AddToCartButton.js — קומפוננט לקוח
 * ===================================================
 * 
 * זהו קומפוננט לקוח (CLIENT COMPONENT) 🖥️
 * שימו לב להוראת "use client" למעלה — חובה!
 * 
 * קומפוננט שרת לעומת קומפוננט לקוח:
 * ┌─────────────────────┬──────────────────────────┐
 * │  קומפוננט שרת       │   קומפוננט לקוח          │
 * ├─────────────────────┼──────────────────────────┤
 * │ רץ על השרת          │ רץ בדפדפן               │
 * │ בלי אינטראקטיביות   │ מטפל בלחיצות, קלט       │
 * │ יכול למשוך נתונים   │ יכול להשתמש ב-useState  │
 * │ בלי useState/Effect │ שולח JavaScript          │
 * │ מהיר יותר (פחות JS) │ נדרש לאינטראקטיביות     │
 * └─────────────────────┴──────────────────────────┘
 * 
 * מתי להשתמש בכל אחד?
 * - צריך כפתור, טופס או אנימציה? → קומפוננט לקוח
 * - רק מציג נתונים? → קומפוננט שרת (ברירת מחדל)
 * 
 * מה הקומפוננט הזה עושה?
 * מציג כפתור שסופר כמה פריטים "הוספת לעגלה".
 * הוא משתמש ב-useState של React כדי לעקוב אחרי הספירה.
 */

"use client"; // ← זה הופך אותו לקומפוננט לקוח!

// ייבוא useState מ-React (עובד רק בקומפוננטי לקוח)
import { useState } from "react";

export default function AddToCartButton({ productName }) {
    // useState יוצר "משתנה מצב" — ערך שיכול להשתנות
    // count = הערך הנוכחי (מתחיל מ-0)
    // setCount = הפונקציה לעדכון שלו
    const [count, setCount] = useState(0);

    // הפונקציה הזו רצה כשהמשתמש לוחץ על הכפתור
    const handleClick = () => {
        // setCount(count + 1) מעדכן את הספירה ומרנדר מחדש את הקומפוננט
        setCount(count + 1);

        // רישום לקונסול הדפדפן (פתח כלי מפתחים → Console כדי לראות)
        console.log(`הוספת ${productName} לעגלה! סה"כ: ${count + 1}`);
    };

    return (
        <button className="btn btn-primary" onClick={handleClick}>
            {/*
        רנדור מותנה:
        - אם count הוא 0: הצג "הוסף לעגלה"
        - אם count > 0: הצג כמה נוספו
      */}
            {count === 0
                ? `🛒 הוסף לעגלה`
                : `🛒 נוסף (${count}) — לחץ שוב!`}
        </button>
    );
}
